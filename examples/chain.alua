-- 演示：async 函数互相 await（add_one 链式调用，期望 (10+5)+5=20）
async function add_one(x)
  local y = await delay(5)
  return x + y
end

async function main()
  local v = await add_one(10)
  local w = await add_one(v)
  return w
end
